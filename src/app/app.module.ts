import { NgModule } from '@angular/core';

// Deprecated: This module is no longer needed in standalone-based Angular applications.
// All components have been converted to standalone mode.
// The application now uses bootstrapApplication() in main.ts instead of BootstrapModule.
// This file can be safely deleted.

@NgModule({
  declarations: [],
  imports: [],
  providers: [],
  bootstrap: []
})
export class AppModule { }
